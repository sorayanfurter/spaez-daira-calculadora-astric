import { object, string, number, boolean, date, array, ObjectSchema } from 'yup';

export default {
    obj: object,
    str: string,
    num: number,
    bol: boolean,
    dat: date,
    arr: array,
    ObjectSchema
}